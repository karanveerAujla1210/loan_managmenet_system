{
  "buildCommand": "cd frontend-unified && npm install && npm run build:vite",
  "outputDirectory": "frontend-unified/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
